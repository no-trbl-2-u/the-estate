---
state: 0007
previous: state/0006.md
date: 2026-08-28
session-verb: "capture"
lenses: []
shape: prose
inputs: []
outputs:
  - ideas/0004-estate-ui/artifacts/0013-deploy-session-residue.md
  - ui/wrangler.jsonc
  - ui/index.html
  - ui/public/
  - ui/scripts/generate.mjs
---

# State 0007

## Established

The deploy walk — state 0006's open question — landed the same day it was
asked. **Estate View is live at https://idea-estate.com** (and www), on
Cloudflare Workers static assets, behind T's own domain, with DNS and
certificates provisioned automatically by the custom-domain routes in
`ui/wrangler.jsonc`. Deploys take seconds. The workers.dev address is retired;
the domain is canonical.

The site was then made search- and machine-legible: full metadata, an OG card
that is a real screenshot of this record's travel map, and — the loop worth
recording — **the generator now publishes the estate as plain text** at
`/llms.txt` and `/llms-full.txt` on every build. Leg 1's law ("the derivation
is proven as plain text first") turned out to be the AI-readiness artifact
verbatim. The publish work itself is steering — no verb in the registry —
so it lives in this snapshot's outputs as paths.

One verb ran, dispatched properly: The Gardener captured the stretch's residue
(`0013-deploy-session-residue.md`).

## Decisions

- None sealed. The deviation Decision from 0006 remains owed, and this stretch
  added a second decide-shaped item beside it (below).

## Tensions

- **The estate is public and invites machine ingestion.** `/llms-full.txt`
  serves every record as text to anyone — including idea-0003's operator
  messages and a friend's business details. Named to T twice, not yet decided.
  The zero-code gate is Cloudflare Access, which is also the recorded v2
  login vision's natural first step. This is `decide`-shaped now, not a note.

## Open questions

- Carried: `relates` 0004↔0001; nothing on this record has faced `challenge`;
  the owed deviation Decision.
- New: whether the near build ever wants server-side rendering — the HTML
  source carries no map content; coverage today is metadata, noscript, and
  the text editions. Nobody has asked for it; recorded so nobody pretends it
  was overlooked.

## Current state declaration

The record's subject now exists in public: the estate can be read at a
bought domain by anyone, human or machine, and the snapshot it serves
includes this very state once regenerated. The natural dispatches, in order
of heat: **`decide`** (two sealed-shaped items now wait — the Seed deviation
and the public-exposure gate), `challenge`, `relate` (0004↔0001).

> What would you like to do next with this idea?
