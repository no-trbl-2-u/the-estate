---
state: 0003
previous: state/0002.md
date: 2026-08-28
session-verb: "chart + phase"
lenses: []
shape: prose
inputs:
  - ideas/0004-estate-ui/artifacts/0003-horizon.md
outputs:
  - ideas/0004-estate-ui/artifacts/0004-trajectory.md
  - ideas/0004-estate-ui/artifacts/0005-phase-1-the-parser-and-the-proof.md
  - ideas/0004-estate-ui/artifacts/0006-phase-2-the-shelf-and-the-open-command.md
  - ideas/0004-estate-ui/artifacts/0007-phase-3-the-travel-map.md
  - ideas/0004-estate-ui/artifacts/0008-phase-4-the-reading-panel-and-the-clean-jump.md
  - ideas/0004-estate-ui/artifacts/0009-phase-5-the-watch.md
---

# State 0003

## Established

The Surveyor ran `chart` and `phase` in one dispatch — both verbs his — under
the fast-track. The Trajectory is classified **`trajectory: actionable`**,
earned: it bottoms out in a Saturday-startable step with zero dependencies.

*(Output filenames above are recorded as the Surveyor's numbering; the five
Phase slugs are his own.)*

### The shape of the route

Five legs sized to afternoons and weekends, obeying both Horizon ordering
constraints — **generator before pixels, shelf before record map** — and shaped
so **"T opens it on real data" happens at the end of leg 2, not the end of the
route.** From leg 3 onward every build session is also a use session. **The
route degrades from the tail:** each leg ends openable, so a low-appetite
answer truncates it cleanly. Legs 1–2 are the minimum viable; leg 4 is the
designated cut point.

| # | Phase | Trigger | Cost |
|---|---|---|---|
| 1 | Parser and proof | none — this Saturday | one Saturday |
| 2 | Shelf + open command | assertions green | one weekend — **ends with T's first real open; F1's clock starts** |
| 3 | Travel map | shelf shipped and opened | two weekends; weekend one ends with a correct ugly map, shippable |
| 4 | Reading panel + clean jump | map renders fixture correctly | one weekend — completes the day-one tier; **the truncation point** |
| 5 | The watch | a month of ordinary use | ~zero build; ends in a one-paragraph verdict |

### Falsifier checkpoints

- **F1** (unopened viewer): armed at Phase 2's close, read in Phase 5 →
  **stop the record, do not iterate.** Cheap early reading at Phase 2: if the
  shelf is not faster than the tree, pause before paying map price.
- **F3** (staleness banner wins): stamp built in Phase 2, read in Phase 5 →
  the pre-named local-server reversal, **proposed to T**.
- **F5** (seam as noise): first possible reading at Phase 3's close →
  pressure travels to **idea-0001 via the `relates` edge, never fixed in this
  record.**
- **F4** (a conjecture renders worse): armed in Phase 3's glyph choice — type
  table verbatim, nothing else — dormant until a non-product record arrives.
- **F2** (the 2026-09-25 clock): a standing station at every leg boundary;
  **T's alone to read.**

## Decisions

- **Routine calls, made as assumptions, all reversible in an afternoon:** home
  at `docs/estate-view/` beside the `idea-pipeline.html` precedent; Node.js
  runtime, hand-rolled parser, anything vendored under
  `docs/estate-view/vendor/`; generated `index.html` committed like
  `SURVEY.md`; open command as `view.ps1`/`view.cmd`.
- **The inbound-citation ruling** (below) — the one place `chart` corrected
  `envision`.

## What the decomposition exposed that the Horizon did not know

**The shelf promises inbound-citation counts the frontmatter cannot pay.**
Lineage derives from `inputs:` only, and idea-0004's artifacts cite only their
own record's files there — the pointing at idea-0001 lives in body prose. The
only cross-record frontmatter edges in the whole fixture set are the Seed's
nine. **The Trajectory rules: the badge computes from `inputs:` alone — body
mentions are not edges; counting them would hand-draw lineage.** So
idea-0001's badge honestly reads **zero** on day one: a third honestly-empty
layer beside the survey panel and `relates`, filled the day `relate` runs.

## Gold nuggets

- **"The route degrades from the tail."**
- **"From leg 3 onward every build session is also a use session."**
- **"Body mentions are not edges; counting them would hand-draw lineage."**
- **F1's re-route: "stop the record, do not iterate."**

## Tensions

- Carried: the steering seam, now with a named pressure route (F5 → idea-0001,
  never fixed here). The `relates` edge itself is still undrawn.

## Open questions

- **`appetite:` is still the placeholder `1`**, stated at the top of the
  Trajectory before anything else: the route is sized to heat T has never set,
  and built so a low answer truncates rather than invalidates. **Leg 1 alone is
  a keepable artifact at appetite zero.**
- Carried: the `relates` edge to idea-0001 (Cartographer, unrun).

## Current state declaration

**The record is Seed-shaped in one day**: Horizon (`falsifiable`), Trajectory
(`actionable`), the next concrete move (the Saturday generator — zero
dependencies, waits on nothing), and refusals in abundance with walls attached.
The provenance stamp is minted at export. **The one honest gap a `seed` run
must surface: appetite is unset, and the Seed would export a route sized to a
placeholder.**

What a reader does first on a Saturday: write `docs/estate-view/generate.mjs`,
point it at idea-0003, and make the challenge branch, the seven-way fan-out,
and the one backward provenance line come out right **in plain text**.

> Fast-track pauses here for T: seed it, challenge it first, or set the
> appetite and then seed.
