---
state: 0008
previous: state/0007.md
date: 2026-08-29
session-verb: "capture"
lenses: []
shape: prose
inputs: []
outputs:
  - ideas/0004-estate-ui/artifacts/0014-link-preview-session-residue.md
  - ui/index.html
  - ui/public/_headers
  - ui/public/apple-touch-icon.png
  - ui/public/site.webmanifest
---

# State 0008

## Established

T reported that texting `idea-estate.com` produced no link preview. **The site
was serving a complete and valid Open Graph card the whole time** — confirmed
from an external fetcher outside this network, and against every scraper
user-agent, both schemes, apex and `www`. The investigation found no fault in
the page and did not resolve the reported symptom.

It did find four unrelated defects and fix them, live now on
https://idea-estate.com: `apple-touch-icon` had pointed at the 1200×630 OG
screenshot rather than a square icon; `Content-Type` carried no charset;
`og:image:secure_url`, `og:image:type`, `og:locale` and `twitter:image:alt`
were absent; and `site.webmanifest` offered no raster icon. Shipped as PRs #15
and #16.

Three operational hazards were recorded that will outlive this stretch
(`0014`, items 3–5): the sandbox's egress proxy terminates TLS, so direct
observation from here describes the proxy rather than Cloudflare and produced
two false leads; the SPA not-found fallback returns `index.html` with `200`
for any missing asset, so a `200` never proves a file deployed; and the
Cloudflare token lacks Workers Routes permission, so `wrangler deploy` fails
at route reconciliation *after* shipping the assets — a failed deploy is still
a deploy.

One verb ran: `capture` (`0014-link-preview-session-residue.md`). It was
written by the Steward inline rather than dispatched to The Gardener, because
agent dispatch is disabled in this session's configuration. That is a
deviation from the estate's writer seam and is named here rather than hidden.

## Decisions

- None sealed. Both decide-shaped items from 0007 remain owed, untouched by
  this stretch.

## Tensions

- **Carried from 0007, undiminished:** the estate is public and invites machine
  ingestion; `/llms-full.txt` still serves idea-0003's contents to anyone. This
  stretch published more, decided nothing, and the item is now two sessions old.
- **New, and about this session rather than the estate:** three successive
  diagnoses were delivered to T as conclusions rather than hypotheses, and each
  correction had to come from T reporting failure again. The `?v=2` test was
  offered as decisive and was not decisive in either direction. Recorded in
  `0014` item 2.
- **New:** the writer seam was crossed — the Steward wrote an artifact. Either
  the seam bends where dispatch is unavailable, or the verb should not have run.
  Unresolved.

## Open questions

- Carried: `relates` 0004↔0001; nothing on this record has faced `challenge`;
  the owed deviation Decision; whether the near build ever wants SSR.
- New and live: why a blue-bubble iMessage carrying only the bare URL renders
  no card. Every server-side explanation available from here is eliminated. The
  pending experiment is T sending the link from a second phone that has never
  opened the domain — a local `LinkPresentation` cache would explain it, and a
  failure there would mean the cause is invisible to this session's vantage
  points.

## Current state declaration

The record's subject is live, correct, and better instrumented than it was —
but this stretch closed no question it opened with, and the reported symptom
stands unexplained pending a test only T can run. The natural dispatches, in
order of heat: **the second-device test** (not a verb — the experiment that
unblocks everything else), then **`decide`** (three sealed-shaped items now:
the Seed deviation, the public-exposure gate, and whether the writer seam bends
when dispatch is unavailable), `challenge`, `relate` (0004↔0001).

> What would you like to do next with this idea?
