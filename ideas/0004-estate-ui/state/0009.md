---
state: 0009
previous: state/0008.md
date: 2026-08-28
session-verb: "chart"
lenses: []
shape: prose
inputs:
  - ideas/0004-estate-ui/artifacts/0010-horizon-explored.md
  - ideas/0004-estate-ui/artifacts/0011-ui-v1-phases.md
  - ideas/0004-estate-ui/artifacts/0004-trajectory.md
outputs:
  - ideas/0004-estate-ui/artifacts/0015-v1-v2-roadmap.md
---

# State 0009

## Established

T asked for the route stated whole: v1 as the full site for one operator over
local data, v2 as the platform — login, users, databased artifacts. The
Surveyor charted it from the explored Horizon's two halves:
`0015-v1-v2-roadmap.md`, a second Trajectory standing beside the sealed one,
subsuming its unfinished remainder into v1 rather than superseding it
silently.

The chart's shape: **v1 is six legs ordered by fuse length** — the Access
gate first (zero code, waits only on the owed exposure `decide`, and
deliberately the v2 login's first rung), the record-keeping catch-up, full-
content reading with a named size threshold, self-publishing regeneration, a
bounded polish pass, and the sealed Seed's watch folded in. **v2 is charted
thinner by design** — Worker API beside the same wrangler config, insert-only
D1 mirroring the data contract so immutability is compiled rather than
obeyed, the generator repurposed as importer with the derivation spine moved
server-side untouched, the auth ladder, dispatches as rows, the chat as the
Steward with F7 armed — and gated on a decided, *used* v1 (F6's reading).

## Decisions

- None sealed. The chart sharpens the queue rather than jumping it: both owed
  decides (Seed deviation; exposure gate) are now v1's explicit first leg.

## Tensions

- Carried: the estate is public and machine-ingestable while the gate
  decision waits.
- Two Trajectories now stand on this record. The chart says which subsumes
  which; only a challenge or a decide can seal that reading.

## Open questions

- Carried: `relates` 0004↔0001; no artifact on this record has faced
  `challenge` — and a Trajectory that will steer months of work is the
  natural first target.

## Current state declaration

The record now holds its route in one artifact: from the live public site at
idea-estate.com to the platform, with the seam law intact and the falsifiers
carried. The natural dispatches: **`decide`** (two items, both v1 leg 1),
`challenge` (0015 has never been attacked), `phase` (0015's v1 legs into
concrete parts when a decide clears the gate).

> What would you like to do next with this idea?
