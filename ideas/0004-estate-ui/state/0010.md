---
state: 0010
previous: state/0009.md
date: 2026-08-28
session-verb: "chart"
lenses: []
shape: prose
inputs:
  - ideas/0004-estate-ui/artifacts/0015-v1-v2-roadmap.md
outputs:
  - ideas/0004-estate-ui/artifacts/0015-v1-v2-roadmap.md
---

# State 0010

## Established

T reviewed the roadmap and added a standing convention for all roadmaps — the
human-attention tag — which was dispatched to The Chancellor and sealed on
idea-0001 (`0008-human-attention-tag.md`, and see idea-0001/state/0014). This
record's roadmap was the first artifact bound by it.

**The Surveyor amended `0015-v1-v2-roadmap.md` in place, before merge.** The
artifact was written earlier in the same session, is committed to an unmerged
branch, and had not landed on `main` or been published; the amendment is
review-response, not the rewriting of a sealed artifact. The artifact says so
in its own body rather than leaving a reader to infer it from a diff, which is
the standard this estate holds — the record should not require a diff to be
read honestly.

### What the tags landed on

**Six of twelve legs** — v1.1 (the Access gate), v1.2 (record-keeping
catch-up), v1.6 (the watch); v2.2 (the auth ladder), v2.5 (chat-as-Steward),
v2.6 (Durable Objects). Neither everything nor nothing, which is the first
evidence the criterion discriminates. The route, the ordering, the refusals and
the falsifier readings are unchanged; only the marking is new.

Two judgment calls the Surveyor surfaced rather than buried, and both are worth
carrying because they are where the convention will be tested next:

- **v1.4, regeneration ergonomics — left untagged, conditionally.** The
  one-command local route is work an agent finishes alone; the GitHub Action
  variant needs T to mint a scoped Cloudflare API token and set a repo secret.
  The leg is untagged with the reasoning written out, and **acquires the tag if
  T takes the Action branch**. A reader who disagrees can see exactly where.
- **v1.5, the polish pass — nearly tagged, then not.** The appetite argument
  would have earned it, but `idea.md` already carries `appetite: 3` hand-set by
  T, so the reserved call was already made. Checked rather than assumed.

### Renumbered on merge

A concurrent Steward session reached `main` first with its own artifact `0014`
(`0014-link-preview-session-residue.md`) and state `0008`. This session's work
was renumbered behind it: the roadmap `0014` → **`0015`**, and these snapshots
`0008`/`0009` → **`0009`/`0010`**. All citations were rewritten; nothing was
overwritten and both sessions' work survives whole. The full account, and the
structural question it raises about two Stewards allocating numbers at once,
is on idea-0001 (`state/0015`).

Their state `0008` names a third decide-shaped item — whether the writer seam
bends when agent dispatch is unavailable — which is **not** carried here as
settled; it is that session's finding on this record, and it stands.

## Decisions

- None on this record. The convention was sealed on idea-0001, where a rule
  governing every record belongs.

## Tensions

- Carried: the estate is public and machine-ingestable while the gate decision
  waits — and that gate is now this roadmap's tagged first leg, so the tension
  and the route agree about where the work starts.
- Carried: two Trajectories stand on this record; 0015 says which subsumes
  which, and only a challenge or a decide can seal that reading.

## Open questions

- Carried: `relates` 0004↔0001; nothing on this record has faced `challenge`,
  and the roadmap that will steer months of work is still the natural target.
- v1.4's conditional tag is an open branch, not an open question — recorded so
  it is not mistaken for a settled non-earner.

## Current state declaration

The roadmap now reads honestly about who can do what: an agent can build,
generate, and deploy the whole of v1's middle, and cannot open the gate at its
front. The two owed decisions are unchanged and unmoved by the Chancellor's
first sitting — an office called once settles only what was dispatched to it.
The natural dispatches: **`decide`** (twice, still), `challenge` (0015 has
never been attacked), `phase` (v1's legs into concrete parts once the gate
decision clears).

> What would you like to do next with this idea?
