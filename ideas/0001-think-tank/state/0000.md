---
state: 0000
date: 2026-08-25
session-skill: "10-question vision interview + workspace build (pre-dates the skills it produced)"
inputs: [VISION.md, BRAINSTORM.md, BUILD-PROMPT.md]
outputs: [system/, .claude/skills/, templates/, playbooks/, docs/adr/]
---

# State 0000

## Established
The full interview outcome, recorded as ADRs 0001–0010 (`docs/adr/`). Core:
extraction-first foundry, Seed as terminal type, typed skills in three
families, classification not certification, immutable copy-forward state with
derived lineage, Steward as gap-deriving router, notice-more portfolio scoring.

## Decisions
See `docs/adr/README.md` — this record's decisions ARE the ADR set.

## Gold nuggets
- "The seed is the real idea."
- "A seed with no baggage" — with a one-line return address.
- Disagreement is invocable, not a tone.
- Grade, never gate.
- Drift usually means the idea is working.
- The connection the machinery cannot make (`relates`).

## Open questions
- The "OpenAI OKF" frontmatter format reference (ADR 0008) — T to clarify.
- Reconciling the protected docs with the extraction-first system (ADR 0010) —
  awaiting T's approval.
- The deferred return path: field reports from downstream repos (ADR 0002).

## Current state declaration
The workspace exists and is operational: Steward + ten skills, system law,
templates, and this record. Nothing has been exported yet. The falsifier clock
runs to 2026-09-25 (`system/FALSIFIERS.md`), and the named failure mode is
building instead of using — the next session on ANY idea matters more than the
next session on this one.

> What would you like to do next with this idea?
