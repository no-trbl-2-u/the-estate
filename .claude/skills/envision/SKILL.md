---
name: envision
description: Elaborate the six-month vision of a framed idea. Transformer, Framing → Horizon. Performed by its bound agent; the Steward normally dispatches this for you.
verb: envision
signature: "Framing → Horizon"
agent: TBD
---

# envision — `Framing → Horizon`

Write what this idea looks like **six months from now, working**: concrete,
elaborated, told from inside the working thing — not a wish and not a spec.

A Horizon should attempt its own falsifier ("what would make this wrong").
If the operator cannot name one yet, produce the Horizon anyway with
`classifiers.horizon: unfalsified` — the label is honest, not a block; a later
`challenge` pass can upgrade it.

Produce one `type: Horizon` artifact, `potential-next-steps` usually
`[challenge, chart]`. Close per protocol.
