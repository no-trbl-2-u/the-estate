---
name: decide
description: Drive toward an explicit, recorded decision and seal it. Transformer, produces a Decision. Performed by The Chancellor; the Steward normally dispatches this for you.
verb: decide
signature: "Tensions → Decision"
agent: chancellor
---

# decide — `Tensions → Decision`

Produce a `Decision` recording four things — **what was decided**, **what was
rejected**, **why**, and **what would reopen it**. Name which tensions it
resolves and which it leaves alive.

The operator may decline to decide; record the choice as open rather than
forcing one. A premature decision is more expensive than an acknowledged
tension, because it looks settled.

## Agent binding (hard)

Performed by **The Chancellor** (`chancellor`) and by no one else. If that agent is
unavailable the verb does not run; the Steward reports the gap.
